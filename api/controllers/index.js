const usersController = require('./users.controller'),
    campsController = require('./users.controller');
    eventsController = require('./events.controller.controller');
    authController = require('./events.auth.controller');

module.exports = {
    usersController,
    campsController,
    eventsController,
    authController
};
