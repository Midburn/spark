extends layout
block wrapper
    #{user.newNav}
    if (user.newNav)
        include top-nav-new
    else
        include top-nav
    page.main.container
        #ajax_indicator.hide
        if (breadcrumbs !== undefined)
            include crumbs

        block content
    include footer
